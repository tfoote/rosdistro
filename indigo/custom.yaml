%YAML 1.1
# ROS distribution file
# see REP 141: http://ros.org/reps/rep-0141.html
---
release_platforms:
  fedora:
  - heisenbug
  ubuntu:
  - saucy
  - trusty
repositories:
  test_deployment:
    release:
      packages:
      - test_deployment_one
      - test_deployment_two
      tags:
        release: release/indigo/{package}/{version}
      url: https://github.com/tfoote/test_deployment-release.git
      version: 0.0.2-0
type: distribution
version: 1
